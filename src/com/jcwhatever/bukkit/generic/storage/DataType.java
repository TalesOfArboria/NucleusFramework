package com.jcwhatever.bukkit.generic.storage;

/**
 * Used by data nodes to specify
 * an expected data type.
 */
public enum DataType {
    INTEGER,
    LONG,
    DOUBLE,
    BOOLEAN,
    STRING,
    UUID,
    LOCATION,
    ITEMSTACKS
}
