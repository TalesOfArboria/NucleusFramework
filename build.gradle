import com.sun.org.apache.xalan.internal.xsltc.compiler.Copy

apply plugin: 'java'

sourceCompatibility = 1.7
baseName = "GenericsLib"
version = '1.0'


repositories {
    mavenCentral()
}

dependencies {
    compile files('C:/JavaRepo/bukkit/bukkit-1.7.9-R0.jar',
                  'C:/JavaRepo/bukkit/BarAPI.jar',
                  'C:/JavaRepo/bukkit/ProtocolLib-3.2.0.jar',
                  'C:/JavaRepo/bukkit/Vault.jar',
                  'C:/JavaRepo/bukkit/WorldEdit.jar')
}

sourceSets {

    main {
        java {
            srcDir 'src'
        }
        /*
        resources {
            srcDir 'src/resources'
        }
        */
    }
}

/*
jar {
    manifest {
        attributes("Implementation-Title": "GenericsLib",
                   "Implementation-Version": version)
    }

    from {
        ["plugin.yml"]
    }
}



task clearJar(type: Delete) {
    delete 'build/libs/ProjectName.jar'
}
*/
task makeJar(type: Jar) {
    from('plugin.yml')
    into('plugin.yml')
    //include('classes.jar')
    //rename ('classes.jar', 'ProjectName.jar')
}

makeJar.dependsOn(build)